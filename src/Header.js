import React from "react";
import { Container, Button } from "react-bootstrap";

const Header = () => {
    return (
        <Container className="app-container__header" fluid>
            <h5>Test App for ALW</h5>
        </Container>
    );
}

export default Header;